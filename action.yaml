name: Tanvir's Custom Action

description: Greet someone with a custom message

inputs:
  who-to-greet:
    description: Who to greet
    required: true
    default: Hello

outputs:
  time:
    description: The time we greeted you

runs:
  using: node20
  main: dist/index.js

